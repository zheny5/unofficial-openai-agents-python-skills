# Task: Runtime Behavior Probe

Write a small Python probe plan for an OpenAI Agents SDK Python runtime question involving sessions, streaming, and failure behavior.

Include:

- a validation matrix with at least one control
- `single-shot`, `repeat-3`, or `warm-up + repeat-N`
- fresh vs reused state
- error, repetition, or cleanup handling

Keep the output in a compact, implementation-ready format. Include a Python code block.
