# Task: Structured Output

Ask the coding agent to generate a Pydantic `DeckSpec` using `output_type`, then pass
the typed `final_output` to a deterministic validator and renderer.

The generated code should not parse JSON from plain text.

