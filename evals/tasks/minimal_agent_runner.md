# Task: Minimal Agent + Runner

Ask the coding agent to create a minimal OpenAI Agents SDK Python app with one `Agent`,
a run entrypoint, environment documentation, and a testable function that returns
`result.final_output`.

The generated code should use the correct `Runner` API and avoid hiding IO or side
effects in prompts.

