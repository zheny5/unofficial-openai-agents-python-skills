# Task: Context-Aware Tool

Ask the coding agent to add two `@function_tool` tools. One tool should use request
context via `RunContextWrapper`; the other should fetch deterministic data by ID.

The generated code should keep tools small, typed, and explicit about side effects.

