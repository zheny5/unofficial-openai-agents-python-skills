# Task: Custom Model Endpoint

Ask the coding agent to wire a self-hosted OpenAI-compatible model endpoint for an
OpenAI Agents SDK Python app.

The generated code should centralize provider setup, use the appropriate model wrapper,
and call out tool-calling and structured-output smoke tests.

