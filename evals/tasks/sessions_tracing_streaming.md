# Task: Sessions, Tracing, Streaming

Ask the coding agent to add conversation continuity, trace metadata, and streamed UI
events to an OpenAI Agents SDK Python workflow.

The generated code should avoid leaking secrets or raw private documents into trace
metadata.

